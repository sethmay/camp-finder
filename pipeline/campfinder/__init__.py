"""Camp Finder data pipeline.

Scrapes/extracts Scouts BSA resident summer-camp data into a canonical, human-reviewed
JSON dataset (``data/councils/*.json``) and compiles it to static assets the frontend
loads (``web/public/data/*.json``). See IMPLEMENTATION.md.
"""

__version__ = "0.1.0"
